# Backend API

This folder hosts the separate backend services for Casa Studio.

- File upload and download endpoints: /api/files and /api/files/download/:name
- Consultation email delivery: /api/send-email
- Health check: /api/health

Run it with:

```bash
npm run dev:backend
```
