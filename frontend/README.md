# Frontend App

This folder is the public-facing frontend entrypoint for Casa Studio.

The UI is still served from the root Vite app, but the backend logic now lives separately in the backend directory to keep the request flow easy to reason about.

Run it with:

```bash
npm run dev:frontend
```
